# Tests

- **test_rag.py** — Pruebas del sistema RAG (búsqueda en la base de conocimiento de seguros). Ejecutar desde la raíz: `python tests/test_rag.py`. Requiere que el vector store esté inicializado (datos en `data/`).
